# Optional. The app works without Secrets. Never commit real credentials.
# Copy this section into Streamlit Cloud > App settings > Secrets.
[llm]
provider = "none"

# Hosted OpenAI Chat Completions (or a compatible HTTPS endpoint):
# provider = "openai"
# model = "YOUR_AVAILABLE_MODEL"
# base_url = "https://api.openai.com/v1"
# api_key = "YOUR_API_KEY"

# Ollama only when an actual server is available to this deployment:
# provider = "ollama"
# model = "YOUR_INSTALLED_MODEL"
# base_url = "https://YOUR_AUTHENTICATED_OLLAMA_SERVICE"
# api_key = "YOUR_SERVICE_TOKEN"
# Local development may use http://127.0.0.1:11434 instead.
